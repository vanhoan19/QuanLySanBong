@model IEnumerable<QuanLySanBong.Models.YardDetail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlayGround)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubYard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeSlot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayGround.PlayGroundId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubYard.SubYardId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeSlot.TimeSlotId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.YardDetailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.YardDetailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.YardDetailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
